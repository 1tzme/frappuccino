# Hot Coffee Application Configuration
# Copy this file to .env and modify the values as needed

# Server Configuration
HOST=localhost
PORT=8080

# Logging Configuration
LOG_OUTPUT=stdout

# Application Environment
ENVIRONMENT=development

# TODO: Transition State: JSON → PostgreSQL
# DEPRECATED: Remove DATA_DIR after migration to PostgreSQL
# This configuration will be replaced by database connection settings
# Data Storage (Legacy - for migration from JSON)
DATA_DIR=./data

# TODO: Transition State: JSON → PostgreSQL  
# PRIORITY: Database configuration will replace JSON file storage
# Ensure all database environment variables are properly configured
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=hotcoffee
DB_SSL_MODE=disable

# Database Connection Pool Settings (optional)
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m
DB_CONN_MAX_IDLE_TIME=30s

# Development overrides (uncomment for local development)
LOG_LEVEL=debug
LOG_FORMAT=json
LOG_ENABLE_COLORS=true
LOG_ENABLE_CALLER=true
